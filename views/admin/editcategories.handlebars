{{#each errors}}
    <div class='alert alert-danger'>{{texto}}</div>
{{else}}
{{/each}}
<h3>Edit Category:</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categories/edit" method="POST">
            <input type="hidden" id ='id' name="id" value="{{category._id}}">
            <label for="name">Name: </label>
                <input type="text" id="name" name="name" placeholder="Enter category name" class="form-control" value="{{category.name}}">
            <label for="slug">Slug: </label>
                <input type="text" id="slug" name="slug" placeholder="Enter slug category" class="form-control" value="{{category.slug}}">
            <br>
            <button type="submit" class="btn btn-success">Modify Category</button>
        </form>
    </div>
</div>